# Fitty Web

React + Vite starter for the Fitty web app.

```bash
npm install
npm run dev
```

Set `VITE_API_BASE_URL=http://localhost:8080` to call the Docker Compose API Gateway. The UI uses the central tokens in `src/theme.ts` and exposes a component showcase through the in-app `UI Kit` navigation item.
