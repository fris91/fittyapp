// Placeholder runtime config served during local `vite dev`.
// In Kubernetes this file is regenerated at container start from environment variables
// (see frontend-web/docker-entrypoint.sh). Keep keys in sync with main.tsx readConfig().
window.__FITTY_ENV__ = window.__FITTY_ENV__ || {};
